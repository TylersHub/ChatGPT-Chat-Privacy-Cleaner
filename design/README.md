# Design references

These generated concepts define ClearSlate's visual system and primary workflow states. They are design references only; all application controls and text are implemented as accessible React components.

- `keywords-concept.png`: keyword composer and optional AI assistance
- `scan-concept.png`: active search progress and safety summary

